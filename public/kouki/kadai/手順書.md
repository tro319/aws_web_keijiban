
# 2025後期最終課題 手順書 (吉田匠太郎_ktc23a32f0005)

## Dockerインストール

まずDockerをインストール

```bash
sudo yum install -y docker
```

インストールしたら、起動

```bash
sudo systemctl start docker
sudo systemctl enable docker
```


デフォルトのユーザーでも、毎回sudoつけずに起動できるように設定

```bash
sudo usermod -a -G docker ec2-user
```

## Docker Composeインストール

次にDocker Composeをインスト－ル

```bash
sudo mkdir -p /usr/local/lib/docker/cli-plugins/
sudo curl -SL https://github.com/docker/compose/releases/download/v2.36.0/docker-compose-linux-x86_64 -o /usr/local/lib/docker/cli-plugins/docker-compose
sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
```

インストール確認のため、Docker Composeのバージョンを確認

```bash
docker compose version
```

## Docker Composeを使ってみる

まず作業用のディレクトリを作成&そこに移動

```bash
mkdir dockerwork
cd dockerwork
```
次に設定ファイルを作成

```bash
vim compose.yml
```

中身の記述は下記

```bash
services:
  web:
    image: nginx:latest
    ports:
      - 80:80
```

記述が終わったら、起動

```bash
docker compose up
```

起動ができたら、http://パブリックIPアドレス と入力

Welcome to nginx! とブラウザに表示されたら成功

停止させたい時は、さっき起動させたウィンドウに戻り Ctrl+C


